#set in python 
# set is the collection of the unordered items 
# Each elements in the set must be unique & immutable .
# improtant point-->> set is mutable but set's element is immutable set store only data type 
# like 'string',int,float,boolean,and tuple not store list and dictionary 
collecation = {55,64.65,"Ritik","Riya",True,False,"Ritik"} # set is unorderd not given valuse in order by 
print(type(collecation))
print(collecation) # set give only unique values not duplicate valuse

# how to create empty set in python let's do it

Empty_set = set() # empty set's 
print(type(Empty_set))
print("Here is ths empty set -->>",Empty_set)